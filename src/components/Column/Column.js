import React from 'react';

const Column = ({ color }) => {
    return (
        <div className="column" style={{ backgroundColor: color }}>
            column
        </div>
    );
};

export default Column;
